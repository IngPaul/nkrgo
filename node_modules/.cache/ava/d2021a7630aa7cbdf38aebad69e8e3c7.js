'use strict';

const test = require('ava');
const utils = require('./src/test/utils');
test('Extracting hashtags from text', t => {
  let tags = utils.extractTags('a ##picture with tags #Awesome #AVA');
  t.deepEqual(tags, ['picture', 'Awesome', 'AVA']);
  tags = utils.extractTags('a picture with no tags');
  t.deepEqual(tags, []);
  tags = utils.extractTags();
  t.deepEqual(tags, []);
  tags = utils.extractTags();
  t.deepEqual(tags, []);
});
//# sourceMappingURL=../node_modules/.cache/ava/d2021a7630aa7cbdf38aebad69e8e3c7.js.map